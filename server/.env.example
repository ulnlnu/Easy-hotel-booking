# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Secret (生产环境请使用强密码)
JWT_SECRET=yisu-hotel-booking-platform-jwt-secret-key-2025
JWT_EXPIRES_IN=7d

# Database (JSON file storage)
DATA_DIR=./src/data

# CORS Allowed Origins (移动端和PC端地址）
# 注意：Taro H5 可能因端口被占用自动切换到其他端口（如 1565, 8080 等）
# 建议添加多个可能的前端端口以避免 CORS 错误
CORS_ORIGINS=http://localhost:5173,http://localhost:10086,http://localhost:1565,http://localhost:8080
