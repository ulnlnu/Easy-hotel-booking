# PC端开发计划 - 成员B（方案A）

> 创建日期：2025-02-13
> 负责人：成员B
> 负责模块：登录/注册页、酒店信息录入页、审核/发布列表页

---

## 📋 两种开发模式对比

### 模式一：前后端并行开发

```
┌─────────────────────────────────────────────────────────────────┐
│                     模式一：前后端并行                        │
├─────────────────────────────────────────────────────────────────┤
│                                                               │
│  Day 1 ─────────────────────────────────────────────────────    │
│  ┌─────────────────┐        ┌─────────────────┐              │
│  │   成员B (PC)    │        │   成员C (后端)  │              │
│  │                 │        │                 │              │
│  │ 等待API就绪... │  ←→   │ Express框架搭建  │              │
│  │                 │        │ API基础接口     │              │
│  └─────────────────┘        └─────────────────┘              │
│                                                               │
│  Day 2 ─────────────────────────────────────────────────────    │
│  ┌─────────────────┐        ┌─────────────────┐              │
│  │ 登录页开发      │  ←→   │ 完善login API   │              │
│  │ 调用真实API    │        │ 处理CORS       │              │
│  └─────────────────┘        └─────────────────┘              │
│                                                               │
└─────────────────────────────────────────────────────────────────┘
```

| 优点 | 缺点 |
|-----|-----|
| 开发完成后即真实可用 | 前端开发受后端进度制约 |
| 联调问题早发现 | 前期前端可能闲置等待 |
| 无需后期替换API代码 | 后端API变更时前端需同步修改 |

**适用场景**：成员C能在Day 1上午提供基础API框架

---

### 模式二：先Mock数据，后期联调（推荐）

```
┌─────────────────────────────────────────────────────────────────┐
│                   模式二：先Mock后联调                        │
├─────────────────────────────────────────────────────────────────┤
│                                                               │
│  Day 1-3 ─────────────────────────────────────────────────    │
│  ┌─────────────────────────────────────────────────────┐        │
│  │                                                     │        │
│  │  PC端独立开发，使用Mock数据                          │        │
│  │  ┌──────────────┐      ┌──────────────┐           │        │
│  │  │ mockApi.ts   │ ───→│  PC端页面    │           │        │
│  │  │              │      │  (完全独立)  │           │        │
│  │  └──────────────┘      └──────────────┘           │        │
│  │                                                     │        │
│  └─────────────────────────────────────────────────────┘        │
│                                                               │
│  Day 4 ─────────────────────────────────────────────────────    │
│  ┌─────────────────┐        ┌─────────────────┐              │
│  │  PC端         │  ←→   │   后端C        │              │
│  │  替换API调用   │        │  API已就绪      │              │
│  │  开始真实联调  │        │                 │              │
│  └─────────────────┘        └─────────────────┘              │
│                                                               │
└─────────────────────────────────────────────────────────────────┘
```

| 优点 | 缺点 |
|-----|-----|
| PC端开发完全独立 | Day 4需替换API调用 |
| 不受后端进度影响 | 需要约定API接口规范 |
| Mock数据即接口文档 | 联调时需要一定工作量 |
| 开发效率更高 | |

**适用场景**：前后端同时开工，推荐使用 ⭐⭐⭐⭐⭐

---

## 🔧 Mock数据实现方案

```typescript
/**
 * services/mockApi.ts
 * Mock API服务，前期开发使用，后期替换为真实API
 */

// Mock数据延迟，模拟网络请求
const delay = (ms: number) => new Promise(resolve => setTimeout(resolve, ms));

// Mock酒店数据
export const mockHotels = [
  {
    id: '1',
    name: '易宿精选酒店（北京朝阳店）',
    address: '北京市朝阳区建国路88号',
    phone: '010-12345678',
    status: 'pending', // pending | approved | rejected | offline
    images: ['https://example.com/1.jpg'],
    roomTypes: [
      { id: 'r1', name: '标准大床房', price: 299, stock: 10 },
      { id: 'r2', name: '豪华双床房', price: 399, stock: 5 },
    ],
    createdAt: '2025-02-10',
  },
  {
    id: '2',
    name: '易宿精选酒店（上海浦东店）',
    address: '上海市浦东新区世纪大道100号',
    phone: '021-87654321',
    status: 'approved',
    images: ['https://example.com/2.jpg'],
    roomTypes: [
      { id: 'r3', name: '商务套房', price: 599, stock: 3 },
    ],
    createdAt: '2025-02-08',
  },
];

// Mock用户数据
export const mockUsers = [
  {
    id: 'u1',
    username: 'admin',
    password: '123456', // 实际项目需要加密
    role: 'admin', // admin | merchant
    name: '管理员',
  },
  {
    id: 'u2',
    username: 'merchant01',
    password: '123456',
    role: 'merchant',
    name: '商户张三',
  },
];

/**
 * 登录API
 * POST /api/auth/login
 */
export const loginApi = async (username: string, password: string) => {
  await delay(500); // 模拟网络延迟

  const user = mockUsers.find(
    u => u.username === username && u.password === password
  );

  if (user) {
    return {
      success: true,
      data: {
        token: `mock-token-${user.id}-${Date.now()}`,
        user: {
          id: user.id,
          username: user.username,
          role: user.role,
          name: user.name,
        },
      },
    };
  }

  return {
    success: false,
    message: '用户名或密码错误',
  };
};

/**
 * 获取酒店列表
 * GET /api/hotels
 */
export const getHotelsApi = async (params?: { status?: string }) => {
  await delay(300);

  let list = mockHotels;
  if (params?.status) {
    list = list.filter(h => h.status === params.status);
  }

  return {
    success: true,
    data: list,
    total: list.length,
  };
};

/**
 * 保存酒店信息（新增或编辑）
 * POST /api/hotels
 * PUT /api/hotels/:id
 */
export const saveHotelApi = async (hotelData: any) => {
  await delay(500);

  const existingIndex = mockHotels.findIndex(h => h.id === hotelData.id);

  if (existingIndex >= 0) {
    // 编辑
    mockHotels[existingIndex] = { ...mockHotels[existingIndex], ...hotelData };
  } else {
    // 新增
    mockHotels.unshift({
      ...hotelData,
      id: `hotel-${Date.now()}`,
      status: 'pending',
      createdAt: new Date().toISOString().split('T')[0],
    });
  }

  return {
    success: true,
    data: hotelData,
    message: existingIndex >= 0 ? '更新成功' : '提交成功，等待审核',
  };
};

/**
 * 审核酒店
 * POST /api/hotels/:id/audit
 */
export const auditHotelApi = async (id: string, action: 'approve' | 'reject') => {
  await delay(300);

  const hotel = mockHotels.find(h => h.id === id);
  if (hotel) {
    hotel.status = action === 'approve' ? 'approved' : 'rejected';
  }

  return {
    success: true,
    message: action === 'approve' ? '审核通过' : '审核拒绝',
  };
};

/**
 * 上架/下架酒店
 * POST /api/hotels/:id/toggle
 */
export const toggleHotelApi = async (id: string, online: boolean) => {
  await delay(300);

  const hotel = mockHotels.find(h => h.id === id);
  if (hotel) {
    hotel.status = online ? 'approved' : 'offline';
  }

  return {
    success: true,
    message: online ? '上架成功' : '下架成功',
  };
};
```

### 后期联调时的替换方案

```typescript
/**
 * services/api.ts
 * 真实API服务，联调时使用
 */
import axios from 'axios';

const apiClient = axios.create({
  baseURL: 'http://localhost:3000/api',
  timeout: 5000,
});

// 请求拦截器：添加token
apiClient.interceptors.request.use(config => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 导出与Mock API同名的函数，方便替换
export const loginApi = (username: string, password: string) =>
  apiClient.post('/auth/login', { username, password });

export const getHotelsApi = (params?: { status?: string }) =>
  apiClient.get('/hotels', { params });

export const saveHotelApi = (hotelData: any) =>
  hotelData.id
    ? apiClient.put(`/hotels/${hotelData.id}`, hotelData)
    : apiClient.post('/hotels', hotelData);

export const auditHotelApi = (id: string, action: string) =>
  apiClient.post(`/hotels/${id}/audit`, { action });

export const toggleHotelApi = (id: string, online: boolean) =>
  apiClient.post(`/hotels/${id}/toggle`, { online });
```

**切换方式**：只需要修改导入语句
```typescript
// 开发阶段
import { loginApi, getHotelsApi } from './services/mockApi';

// 联调阶段（只改这一行！）
import { loginApi, getHotelsApi } from './services/api';
```

---

## 📅 模式一详细计划（前后端并行）

### Week 1

| 天数 | 开发任务 | 详细内容 | Git分支 | Commit示例 |
|-----|---------|---------|---------|-----------|
| **Day 1** | 环境搭建 + 项目初始化 | 1. 创建React+Vite项目<br>2. 安装Ant Design<br>3. 配置Router<br>4. 等待后端API | `feature/setup-admin` | `feat: 初始化PC端项目，配置Vite+React+TS`<br>`feat: 安装并配置Ant Design和React Router` |
| **Day 2** | 登录/注册页框架 | 1. 登录页UI（Ant Form）<br>2. 注册页UI<br>3. 调用后端login API<br>4. Token存储 | `feature/login-page` | `feat: 实现登录页面UI`<br>`feat: 实现注册页面UI`<br>`feat: 集成登录API和token存储` |
| **Day 3** | 登录逻辑完善 | 1. 表单验证<br>2. 角色区分<br>3. 路由守卫 | `feature/login-page` | `feat: 添加表单验证和角色区分`<br>`feat: 实现路由守卫，未登录跳转` |
| **Day 4** | 酒店录入页框架 | 1. 页面布局<br>2. 基础表单字段<br>3. 调用后端API | `feature/hotel-edit` | `feat: 创建酒店录入页面框架`<br>`feat: 实现基础表单字段` |
| **Day 5** | 酒店表单完善 | 1. 图片上传组件<br>2. 房型列表编辑<br>3. 表单验证规则 | `feature/hotel-edit` | `feat: 实现图片上传组件`<br>`feat: 实现房型列表编辑功能`<br>`feat: 添加表单验证规则` |
| **Day 6** | 审核列表页 | 1. Table展示酒店列表<br>2. 状态筛选<br>3. 操作按钮 | `feature/audit-list` | `feat: 实现审核列表页面`<br>`feat: 添加状态筛选功能` |
| **Day 7** | 状态流转逻辑 | 1. 审核/拒绝功能<br>2. 上架/下架<br>3. 操作反馈 | `feature/audit-list` | `feat: 实现审核/拒绝操作`<br>`feat: 实现上架/下架功能`<br>`feat: 添加操作反馈提示` |

### Week 2

| 天数 | 开发任务 | 详细内容 | Git分支 | Commit示例 |
|-----|---------|---------|---------|-----------|
| **Day 8** | UI美化 | 1. 整体样式优化<br>2. 添加过渡动画<br>3. 响应式调整 | `feature/ui-polish` | `style: 优化登录页面样式`<br>`style: 添加页面过渡动画`<br>`style: 调整响应式布局` |
| **Day 9** | 表单交互优化 | 1. 表单加载状态<br>2. 错误提示优化<br>3. 快捷键支持 | `feature/ui-polish` | `feat: 添加表单加载状态`<br>`feat: 优化错误提示交互` |
| **Day 10** | 数据联动测试 | 1. PC端保存→移动端查看<br>2. 状态同步测试<br>3. 修复联调问题 | `feature/bugfix-sync` | `fix: 修复数据同步问题`<br>`fix: 修复状态更新延迟` |
| **Day 11** | 可选功能 | 1. 数据导出功能<br>2. 批量操作<br>3. 搜索功能 | `feature/export-data` | `feat: 添加数据导出功能`<br>`feat: 实现批量操作` |
| **Day 12** | PC端测试 | 1. 功能测试<br>2. 兼容性测试<br>3. 性能测试 | - | `test: 完成PC端功能测试`<br>`test: 完成浏览器兼容性测试` |
| **Day 13** | Bug修复 | 1. 修复测试发现的问题<br>2. 代码优化<br>3. 注释完善 | `bugfix/day13` | `fix: 修复表单验证问题`<br>`refactor: 优化API调用逻辑` |
| **Day 14** | 演示准备 | 1. 准备演示数据<br>2. 编写演示脚本<br>3. 文档完善 | - | `docs: 完善PC端文档`<br>`chore: 准备演示环境` |

---

## 📅 模式二详细计划（先Mock后联调，推荐）

### Week 1

| 天数 | 开发任务 | 详细内容 | Git分支 | Commit示例 |
|-----|---------|---------|---------|-----------|
| **Day 1** | 环境搭建 + Mock数据 | 1. 创建React+Vite项目<br>2. 安装Ant Design<br>3. **创建mockApi.ts**<br>4. 配置Router | `feature/setup-admin` | `feat: 初始化PC端项目`<br>`feat: 安装并配置Ant Design`<br>`feat: 创建Mock API服务` |
| **Day 2** | 登录/注册页（Mock） | 1. 登录页UI<br>2. 注册页UI<br>3. **调用loginApi（Mock）**<br>4. Token存储 | `feature/login-page` | `feat: 实现登录页面UI`<br>`feat: 实现注册页面UI`<br>`feat: 集成Mock登录API` |
| **Day 3** | 登录逻辑完善 | 1. 表单验证<br>2. 角色区分<br>3. 路由守卫 | `feature/login-page` | `feat: 添加表单验证和角色区分`<br>`feat: 实现路由守卫` |
| **Day 4** | **【联调开始】**替换API | 1. **创建真实api.ts**<br>2. **替换import语句**<br>3. 调试真实登录 | `feature/login-page` | `refactor: 创建真实API服务`<br>`refactor: 替换Mock API为真实API`<br>`fix: 修复登录联调问题` |
| **Day 5** | 酒店录入页框架 | 1. 页面布局<br>2. 基础表单字段<br>3. **调用saveHotelApi** | `feature/hotel-edit` | `feat: 创建酒店录入页面框架`<br>`feat: 实现基础表单字段`<br>`feat: 集成酒店保存API` |
| **Day 6** | 酒店表单完善 | 1. 图片上传组件<br>2. 房型列表编辑<br>3. 表单验证规则 | `feature/hotel-edit` | `feat: 实现图片上传组件`<br>`feat: 实现房型列表编辑功能`<br>`feat: 添加表单验证规则` |
| **Day 7** | 审核列表页 | 1. Table展示酒店列表<br>2. 状态筛选<br>3. 操作按钮 | `feature/audit-list` | `feat: 实现审核列表页面`<br>`feat: 添加状态筛选功能` |

### Week 2

| 天数 | 开发任务 | 详细内容 | Git分支 | Commit示例 |
|-----|---------|---------|---------|-----------|
| **Day 8** | 状态流转逻辑 | 1. 审核/拒绝功能<br>2. 上架/下架<br>3. 操作反馈 | `feature/audit-list` | `feat: 实现审核/拒绝操作`<br>`feat: 实现上架/下架功能`<br>`feat: 添加操作反馈提示` |
| **Day 9** | UI美化 | 1. 整体样式优化<br>2. 添加过渡动画<br>3. 响应式调整 | `feature/ui-polish` | `style: 优化整体页面样式`<br>`style: 添加页面过渡动画` |
| **Day 10** | 表单交互优化 | 1. 表单加载状态<br>2. 错误提示优化<br>3. 快捷键支持 | `feature/ui-polish` | `feat: 添加表单加载状态`<br>`feat: 优化错误提示交互` |
| **Day 11** | 数据联动测试 | 1. PC端保存→移动端查看<br>2. 状态同步测试<br>3. 修复联调问题 | `feature/bugfix-sync` | `fix: 修复数据同步问题`<br>`fix: 修复状态更新延迟` |
| **Day 12** | PC端测试 | 1. 功能测试<br>2. 兼容性测试<br>3. 性能测试 | - | `test: 完成PC端功能测试`<br>`test: 完成浏览器兼容性测试` |
| **Day 13** | Bug修复 | 1. 修复测试发现的问题<br>2. 代码优化<br>3. 注释完善 | `bugfix/day13` | `fix: 修复表单验证问题`<br>`refactor: 优化API调用逻辑` |
| **Day 14** | 演示准备 | 1. 准备演示数据<br>2. 编写演示脚本<br>3. 文档完善 | - | `docs: 完善PC端文档`<br>`chore: 准备演示环境` |

---

## 🌳 Git分支策略

### 分支命名规范

```
main                    (主分支，保护)
  ↓
feature/功能名          (功能分支)
  ↓
PR (Pull Request)       (代码审查)
  ↓
合并到main
```

### 成员B的分支列表

```
main (主分支)
├── feature/setup-admin      # 环境搭建
├── feature/login-page      # 登录/注册页
├── feature/hotel-edit     # 酒店录入页
├── feature/audit-list     # 审核列表页
├── feature/ui-polish      # UI美化
├── feature/export-data    # 数据导出(可选)
├── feature/bugfix-sync    # 数据联调修复
└── bugfix/day13         # Bug修复
```

### 分支操作流程

```bash
# 1. 拉取最新main分支
git checkout main
git pull origin main

# 2. 创建功能分支
git checkout -b feature/login-page

# 3. 开发并提交
git add .
git commit -m "feat: 实现登录页面UI"

# 4. 推送到远程
git push origin feature/login-page

# 5. 创建PR (在GitHub网页操作)
# 6. 代码审查通过后合并到main
# 7. 删除已合并的分支
git branch -d feature/login-page
```

### 合并时间节点

| 日期 | 合并分支 | 检查点 |
|-----|---------|--------|
| Day 1 晚上 | `feature/setup-admin` | M1: 环境搭建完成 |
| Day 3 晚上 | `feature/login-page` | 登录功能可用 |
| Day 6 晚上 | `feature/hotel-edit` | 酒店录入功能可用 |
| Day 7 晚上 | `feature/audit-list` | M3: 所有基础页面完成 |
| Day 9 晚上 | `feature/ui-polish` | UI美化完成 |
| Day 11 晚上 | `feature/bugfix-sync` | 数据联调通过 |
| Day 13 晚上 | `bugfix/day13` | Bug修复完成 |

---

## 📝 Conventional Commits 规范

### 提交信息格式

```
<类型>(<范围>): <简短描述>

<详细描述>

<Footer>
```

### 类型 (type)

| 类型 | 说明 | 示例 |
|-----|------|-----|
| `feat` | 新功能 | `feat: 实现登录页面` |
| `fix` | Bug修复 | `fix: 修复表单验证错误` |
| `style` | 样式修改 | `style: 优化按钮样式` |
| `refactor` | 代码重构 | `refactor: 重构API调用逻辑` |
| `perf` | 性能优化 | `perf: 优化列表渲染性能` |
| `test` | 测试相关 | `test: 添加登录单元测试` |
| `docs` | 文档修改 | `docs: 更新API文档` |
| `chore` | 构建/工具修改 | `chore: 更新依赖版本` |

### 范围 (scope)

- `login`: 登录/注册相关
- `hotel-edit`: 酒店录入相关
- `audit`: 审核列表相关
- `api`: API相关
- `ui`: UI组件相关

### 提交示例

```bash
# 新功能
git commit -m "feat(login): 实现登录页面UI和表单验证"

# Bug修复
git commit -m "fix(hotel-edit): 修复图片上传失败问题"

# 样式修改
git commit -m "style: 优化审核列表页面样式"

# 重构
git commit -m "refactor(api): 将Mock API替换为真实API"

# 测试
git commit -m "test: 添加登录表单验证测试"
```

### 多行提交（推荐）

```bash
git commit -m "feat(hotel-edit): 实现酒店信息录入功能

- 实现基础表单字段
- 添加房型列表编辑功能
- 集成图片上传组件
- 添加表单验证规则

Closes #123"
```

---

## 🎯 每日检查清单

### Day 1 检查清单
- [ ] Vite项目能正常启动
- [ ] Ant Design组件正常显示
- [ ] 路由配置完成
- [ ] Mock API或真实API可用
- [ ] Git分支已创建并推送

### Day 2-3 检查清单
- [ ] 登录页面UI完成
- [ ] 注册页面UI完成
- [ ] 登录逻辑正常工作
- [ ] Token能正确存储
- [ ] 表单验证正常工作

### Day 4-6 检查清单
- [ ] 酒店录入页面UI完成
- [ ] 图片上传功能正常
- [ ] 房型列表可正常编辑
- [ ] 表单验证规则生效
- [ ] 数据能正常保存（Mock或真实API）

### Day 7 检查清单
- [ ] 审核列表页面UI完成
- [ ] 状态筛选功能正常
- [ ] 操作按钮显示正确

### Day 8-9 检查清单
- [ ] 状态流转逻辑正常
- [ ] 操作反馈提示正常
- [ ] UI美化完成
- [ ] 动画效果正常

### Day 10-11 检查清单
- [ ] 数据同步测试通过
- [ ] PC端保存后移动端能看到
- [ ] 状态更新延迟正常
- [ ] 无严重Bug

### Day 12-14 检查清单
- [ ] 所有功能测试通过
- [ ] 兼容性测试通过
- [ ] 演示数据准备完成
- [ ] 文档完善
- [ ] 代码整洁无warning

---

## 📊 成员B工作量评估

| 功能模块 | 工作量 | 占比 |
|---------|--------|------|
| 环境搭建 | 0.5天 | 3.6% |
| 登录/注册页 | 2.5天 | 17.9% |
| 酒店信息录入页 | 4天 | 28.6% |
| 审核/发布列表页 | 3天 | 21.4% |
| UI美化 | 2天 | 14.3% |
| 联调+测试 | 1.5天 | 10.7% |
| Bug修复+文档 | 0.5天 | 3.6% |
| **总计** | **14天** | **100%** |

---

## 🔗 与其他成员的协作节点

| 时间 | 与成员C（后端）协作 | 与成员A（移动端）协作 |
|-----|-------------------|-------------------|
| Day 1 | 确认API接口规范 | - |
| Day 4 | 联调登录/酒店API | - |
| Day 7 | 联调审核API | - |
| Day 11 | - | 测试PC端保存→移动端查看 |
| Day 14 | 集成测试验收 | 集成测试验收 |

---

## 📁 推荐的PC端目录结构

```
admin/
├── public/
│   └── favicon.ico
├── src/
│   ├── assets/              # 静态资源
│   ├── components/          # 公共组件
│   │   ├── PageHeader/      # 页面头部组件
│   │   ├── ImageUpload/     # 图片上传组件
│   │   └── RoomTypeEditor/ # 房型编辑组件
│   ├── pages/              # 页面
│   │   ├── Login/           # 登录/注册页
│   │   │   ├── index.tsx
│   │   │   ├── LoginForm.tsx
│   │   │   └── RegisterForm.tsx
│   │   ├── HotelEdit/       # 酒店录入页
│   │   │   ├── index.tsx
│   │   │   └── HotelForm.tsx
│   │   ├── AuditList/       # 审核列表页
│   │   │   ├── index.tsx
│   │   │   └── AuditTable.tsx
│   │   └── Layout/         # 布局页
│   │       └── index.tsx
│   ├── services/           # API服务
│   │   ├── mockApi.ts      # Mock API
│   │   ├── api.ts         # 真实API（联调时切换）
│   │   └── types.ts      # API类型定义
│   ├── hooks/             # 自定义hooks
│   ├── utils/             # 工具函数
│   ├── router/            # 路由配置
│   │   └── index.tsx
│   ├── App.tsx
│   └── main.tsx
├── index.html
├── package.json
├── tsconfig.json
└── vite.config.ts
```

---

> 文档版本：v1.0
> 最后更新：2025-02-13
> 状态：待确认后开始执行
