# 后端+公共模块开发计划 - 成员C（方案A）

> 创建日期：2025-02-13
> 负责人：成员C
> 负责模块：Express后端API、数据结构设计、自定义日历组件、前后端联调、集成测试
> 创新功能：自定义日历组件

---

## 📋 角色定位

```
┌─────────────────────────────────────────────────────────────────┐
│                    成员C：后端+联调负责人                     │
├─────────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │                                                     │    │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────┐  │    │
│  │  │ Express API  │  │ 数据结构    │  │ 公共组件│  │    │
│  │  │ 开发         │  │ 设计        │  │ 日历    │  │    │
│  │  └─────────────┘  └─────────────┘  └─────────┘  │    │
│  │                                                     │    │
│  │  ┌─────────────────────────────────────────────┐       │    │
│  │  │ 前后端联调协调                          │       │    │
│  │  │ • API文档输出                              │       │    │
│  │  │ • Mock数据确认                            │       │    │
│  │  │ • 联调问题解决                           │       │    │
│  │  └─────────────────────────────────────────────┘       │    │
│  │                                                     │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                               │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 核心职责

### 1. Express后端API开发
- 设计RESTful API接口
- 实现用户认证（登录/注册）
- 实现酒店CRUD操作
- 实现审核/上架/下架功能
- 配置CORS跨域

### 2. 数据结构设计
- 用户数据模型
- 酒店数据模型
- 房型数据模型
- JSON文件存储方案

### 3. 公共组件开发
- 自定义日历组件（共享给移动端和PC端）
- 共享TypeScript类型定义
- 共享常量配置

### 4. 前后端联调协调
- 输出API接口文档
- 与成员A（移动端）联调
- 与成员B（PC端）联调
- 解决联调问题

### 5. 集成测试
- 端到端测试
- 数据流测试
- 最终集成验收

### 6. README文档
- 项目说明
- 技术栈介绍
- 开发指南
- API文档

---

## 📅 两周详细开发计划

### Week 1：基础搭建 + 核心API

| 天数 | 开发任务 | 详细内容 | Git分支 | Commit示例 |
|-----|---------|---------|---------|-----------|
| **Day 1** | 环境搭建 + 数据结构设计 | 1. 初始化Express项目<br>2. 设计数据模型<br>3. 创建JSON数据文件<br>4. 配置开发环境 | `feature/setup-server` | `feat: 初始化Express项目`<br>`feat: 设计用户和酒店数据模型`<br>`feat: 配置开发环境` |
| **Day 2** | 用户认证API | 1. **POST /api/auth/login**<br>2. **POST /api/auth/register**<br>3. Token生成<br>4. 中间件：身份验证 | `feature/auth-api` | `feat: 实现登录API`<br>`feat: 实现注册API`<br>`feat: 实现JWT token验证` |
| **Day 3** | 酒店CRUD API（基础） | 1. **GET /api/hotels**（列表）<br>2. **GET /api/hotels/:id**（详情）<br>3. **POST /api/hotels**（新增）<br>4. **PUT /api/hotels/:id**（编辑） | `feature/hotel-api` | `feat: 实现酒店列表API`<br>`feat: 实现酒店详情API`<br>`feat: 实现酒店新增API`<br>`feat: 实现酒店编辑API` |
| **Day 4** | 酒店管理API + CORS | 1. **DELETE /api/hotels/:id**（删除）<br>2. **POST /api/hotels/:id/audit**（审核）<br>3. **POST /api/hotels/:id/toggle**（上架下架）<br>4. 配置CORS | `feature/hotel-api` | `feat: 实现酒店删除API`<br>`feat: 实现审核API`<br>`feat: 实现上架下架API`<br>`feat: 配置CORS跨域` |
| **Day 5** | **【创新】日历组件开发** | 1. **日历组件框架**<br>2. 日期选择逻辑<br>3. 入住/离店联动<br>4. 间夜计算<br>5. 放到shared目录 | `feature/calendar` | `feat: 实现日历组件框架`<br>`feat: 实现日期选择和联动`<br>`feat: 实现间夜自动计算` |
| **Day 6** | 前后端联调（移动端） | 1. **与成员A联调**<br>2. 修复API问题<br>3. 验证Mock数据结构 | `feature/integration` | `fix: 修复移动端联调问题`<br>`test: 验证移动端API调用` |
| **Day 7** | 前后端联调（PC端） | 1. **与成员B联调**<br>2. 修复API问题<br>3. 验证数据同步 | `feature/integration` | `fix: 修复PC端联调问题`<br>`test: 验证PC端API调用` |

### Week 2：创新功能 + 优化完善

| 天数 | 开发任务 | 详细内容 | Git分支 | Commit示例 |
|-----|---------|---------|---------|-----------|
| **Day 8** | **【创新】LBS定位API** | 1. **GET /api/hotels/nearby**<br>2. 距离计算逻辑<br>3. "距我最近"排序 | `feature/location-api` | `feat: 实现附近酒店API`<br>`feat: 实现距离计算和排序` |
| **Day 9** | 日历组件完善 | 1. 已售罄日期标记<br>2. 价格日历视图<br>3. H5/小程序适配 | `feature/calendar` | `feat: 实现已售罄日期标记`<br>`feat: 实现价格日历视图`<br>`feat: 适配H5和小程序` |
| **Day 10** | API优化 + 错误处理 | 1. 统一错误处理<br>2. 参数验证<br>3. API响应格式统一 | `feature/api-optimize` | `refactor: 优化API错误处理`<br>`feat: 添加参数验证` |
| **Day 11** | 小程序端适配（可选） | 1. 配置小程序域名<br>2. 测试小程序调用<br>3. 适配问题修复 | `feature/weapp-adapter` | `feat: 配置小程序域名白名单`<br>`fix: 修复小程序API调用问题` |
| **Day 12** | 端到端测试 | 1. 完整流程测试<br>2. 数据流测试<br>3. 性能测试 | - | `test: 完成端到端测试`<br>`test: 完成数据流测试` |
| **Day 13** | Bug修复 + README | 1. 修复测试问题<br>2. 代码优化<br>3. **编写README** | `bugfix/day13` | `fix: 修复测试发现的问题`<br>`docs: 编写项目README` |
| **Day 14** | 最终检查 + 演示准备 | 1. 最终集成检查<br>2. 演示数据准备<br>3. 文档完善 | - | `docs: 完善API文档`<br>`chore: 准备演示环境` |

---

## 🔌 Express后端架构设计

### 项目结构

```
server/
├── src/
│   ├── routes/                # 路由
│   │   ├── auth.ts           # 认证路由
│   │   ├── hotels.ts         # 酒店路由
│   │   └── index.ts         # 路由汇总
│   ├── controllers/          # 控制器
│   │   ├── auth.ts
│   │   └── hotels.ts
│   ├── services/            # 业务逻辑
│   │   ├── auth.ts
│   │   └── hotels.ts
│   ├── middleware/         # 中间件
│   │   ├── auth.ts         # 身份验证
│   │   ├── cors.ts         # 跨域
│   │   └── error.ts        # 错误处理
│   ├── data/               # JSON数据存储
│   │   ├── users.json
│   │   ├── hotels.json
│   │   └── .gitkeep
│   ├── types/              # 类型定义
│   │   └── index.ts
│   ├── utils/             # 工具函数
│   │   ├── file.ts         # 文件操作
│   │   ├── jwt.ts         # Token工具
│   │   └── validation.ts   # 参数验证
│   ├── config.ts           # 配置
│   └── index.ts           # 入口文件
├── package.json
├── tsconfig.json
└── nodemon.json           # 热重载配置
```

### API接口设计

```typescript
/**
 * API接口文档
 * Base URL: http://localhost:3000/api
 */

// ========== 认证 ==========

// POST /api/auth/login
// 登录
Request: { username: string, password: string }
Response: {
  success: true,
  data: { token: string, user: { id, username, role, name } }
}

// POST /api/auth/register
// 注册
Request: { username: string, password: string, name: string, role: 'admin'|'merchant' }
Response: { success: true, data: { token: string, user: {...} } }

// GET /api/auth/me
// 获取当前用户信息
Headers: Authorization: Bearer {token}
Response: { success: true, data: { user } }

// ========== 酒店 ==========

// GET /api/hotels
// 获取酒店列表（支持分页、筛选、排序）
Query: {
  page?: number,
  pageSize?: number,
  status?: 'pending'|'approved'|'rejected'|'offline',
  keyword?: string,
  sortBy?: 'price'|'distance'|'rating'|'createdAt'
}
Response: {
  success: true,
  data: [...],
  total: number,
  hasMore: boolean
}

// GET /api/hotels/nearby
// 获取附近酒店（LBS定位）
Query: {
  lat: number,
  lng: number,
  radius?: number  // 半径（公里），默认10
}
Response: {
  success: true,
  data: [...]  // 按距离排序
}

// GET /api/hotels/:id
// 获取酒店详情
Response: { success: true, data: { hotel } }

// POST /api/hotels
// 新增酒店（需要登录）
Headers: Authorization: Bearer {token}
Request: {
  name: string,
  address: string,
  phone: string,
  images: string[],
  roomTypes: Array<{ name, price, area, stock }>,
  ...
}
Response: { success: true, data: { hotel }, message: '提交成功，等待审核' }

// PUT /api/hotels/:id
// 编辑酒店（需要登录，只能编辑自己的）
Headers: Authorization: Bearer {token}
Request: { ...hotelData }
Response: { success: true, data: { hotel }, message: '更新成功' }

// DELETE /api/hotels/:id
// 删除酒店（需要登录）
Headers: Authorization: Bearer {token}
Response: { success: true, message: '删除成功' }

// POST /api/hotels/:id/audit
// 审核酒店（管理员）
Headers: Authorization: Bearer {token}
Request: { action: 'approve'|'reject' }
Response: { success: true, message: '审核通过/拒绝' }

// POST /api/hotels/:id/toggle
// 上架/下架酒店（管理员）
Headers: Authorization: Bearer {token}
Request: { online: boolean }
Response: { success: true, message: '上架成功/下架成功' }
```

---

## 📊 数据结构设计

### 用户数据模型

```typescript
// data/users.json
[
  {
    "id": "u1",
    "username": "admin",
    "password": "$2b$10$...", // bcrypt加密
    "name": "管理员",
    "role": "admin", // admin | merchant
    "createdAt": "2025-02-13T00:00:00.000Z"
  },
  {
    "id": "u2",
    "username": "merchant01",
    "password": "$2b$10$...",
    "name": "商户张三",
    "role": "merchant",
    "createdAt": "2025-02-13T00:00:00.000Z"
  }
]
```

### 酒店数据模型

```typescript
// data/hotels.json
[
  {
    "id": "h1",
    "merchantId": "u2", // 所属商户
    "name": "易宿精选酒店（北京朝阳店）",
    "address": "北京市朝阳区建国路88号",
    "phone": "010-12345678",
    "status": "approved", // pending | approved | rejected | offline
    "images": [
      "https://example.com/hotel1.jpg",
      "https://example.com/hotel2.jpg"
    ],
    "tags": ["近地铁", "免费停车", "含早餐"],
    "facilities": ["WiFi", "空调", "热水器", "电视"],
    "location": {
      "lat": 39.9042,
      "lng": 116.4074
    },
    "roomTypes": [
      {
        "id": "r1",
        "name": "标准大床房",
        "price": 299,
        "originalPrice": 399,
        "area": "25㎡",
        "stock": 10,
        "facilities": ["WiFi", "空调", "电视"]
      },
      {
        "id": "r2",
        "name": "豪华双床房",
        "price": 399,
        "originalPrice": 499,
        "area": "35㎡",
        "stock": 5,
        "facilities": ["WiFi", "空调", "冰箱"]
      }
    ],
    "rating": 4.8,
    "reviewCount": 128,
    "createdAt": "2025-02-10T00:00:00.000Z",
    "updatedAt": "2025-02-13T00:00:00.000Z"
  }
]
```

---

## 🧩 自定义日历组件（创新功能）

### 功能需求

| 功能 | 说明 | 对应评分 |
|-----|------|---------|
| 日期选择 | 入住日期、离店日期选择 | 用户体验 |
| 日期联动 | 离店日期自动晚于入住日期 | 用户体验 |
| 间夜计算 | 自动计算入住天数 | 用户体验 |
| 已售罄标记 | 标记无房日期 | 用户体验 |
| 价格日历 | 不同日期显示不同价格 | 技术复杂度 |
| H5/小程序适配 | 两端兼容 | 技术复杂度 |

### 组件结构

```tsx
/**
 * shared/components/Calendar/
 * 自定义日历组件
 */
import { useState, useMemo } from 'react';
import { View, Text } from '@tarojs/components';
import dayjs from 'dayjs';

interface CalendarProps {
  value?: [Date, Date]; // [入住日期, 离店日期]
  onChange?: (dates: [Date, Date]) => void;
  soldOutDates?: Date[]; // 已售罄日期
  priceData?: Record<string, number>; // 价格数据 { '2025-02-14': 299 }
  minDate?: Date;
  maxDate?: Date;
}

export const Calendar: React.FC<CalendarProps> = ({
  value,
  onChange,
  soldOutDates = [],
  priceData = {},
  minDate = new Date(),
  maxDate,
}) => {
  const [currentMonth, setCurrentMonth] = useState(dayjs());

  // 计算间夜
  const nights = useMemo(() => {
    if (!value || !value[0] || !value[1]) return 0;
    return dayjs(value[1]).diff(dayjs(value[0]), 'day');
  }, [value]);

  // 渲染日期网格
  const renderDays = () => {
    // ... 日期渲染逻辑
  };

  // 处理日期点击
  const handleDateClick = (date: Date) => {
    // ... 日期选择逻辑（联动校验）
    if (!value || !value[0]) {
      // 选择入住日期
      onChange?.([date, null as any]);
    } else if (!value[1]) {
      // 选择离店日期
      if (dayjs(date).isAfter(dayjs(value[0]))) {
        onChange?.([value[0], date]);
      }
    } else {
      // 重新选择
      onChange?.([date, null as any]);
    }
  };

  return (
    <View className="calendar">
      {/* 月份头部 */}
      {/* 星期表头 */}
      {/* 日期网格 */}
      {/* 间夜显示 */}
      {nights > 0 && (
        <Text className="calendar-nights">共 {nights} 晚</Text>
      )}
    </View>
  );
};
```

### 使用示例

```tsx
// 移动端使用
import { Calendar } from '../../../shared/components/Calendar';

const HomePage = () => {
  const [dateRange, setDateRange] = useState<[Date, Date]>([null, null]);

  return (
    <Calendar
      value={dateRange}
      onChange={setDateRange}
      soldOutDates={[new Date('2025-02-14')]}
      priceData={{ '2025-02-15': 299, '2025-02-16': 399 }}
    />
  );
};
```

---

## 🌳 Git分支策略

### 分支列表

```
main (主分支)
├── feature/setup-server       # 环境搭建
├── feature/auth-api          # 认证API
├── feature/hotel-api        # 酒店API
├── feature/calendar          # 日历组件（创新）
├── feature/location-api      # LBS定位API（创新）
├── feature/integration      # 前后端联调
├── feature/api-optimize     # API优化
├── feature/weapp-adapter   # 小程序适配
└── bugfix/day13          # Bug修复
```

### 合并时间节点

| 日期 | 合并分支 | 检查点 |
|-----|---------|--------|
| Day 1 晚上 | `feature/setup-server` | M1: 环境搭建完成 |
| Day 2 晚上 | `feature/auth-api` | 认证API可用 |
| Day 4 晚上 | `feature/hotel-api` | M2: 后端API可用 |
| Day 5 晚上 | `feature/calendar` | 日历组件完成 |
| Day 7 晚上 | `feature/integration` | M3: 前后端联调通过 |
| Day 9 晚上 | `feature/location-api` | M4: 创新功能完成 |
| Day 13 晚上 | `bugfix/day13` | Bug修复完成 |
| Day 14 | 所有功能分支 | M6: 最终集成验收 |

---

## 🤝 前后端联调流程

### 与成员A（移动端）联调

```
Day 4 - API接口确认
┌─────────────────────────────────────────────────────────────┐
│  成员C（后端）          成员A（移动端）              │
│  ┌─────────────┐        ┌─────────────┐             │
│  │ 输出API文档 │  ───→  │ 确认接口规范│             │
│  └─────────────┘        └─────────────┘             │
│         ↑                                            │
│         │                                            │
│  Postman测试确认可调用                          │
└─────────────────────────────────────────────────────────────┘

Day 6 - 实际联调
┌─────────────────────────────────────────────────────────────┐
│  成员C                        成员A                │
│  ┌─────────────┐              ┌─────────────┐        │
│  │ 后端服务    │  ←←←←←←←←→│  替换Mock  │        │
│  │ localhost:3 │              │  为真实API  │        │
│  └─────────────┘              └─────────────┘        │
│         ↑                                            │
│         │ 调试问题                                  │
│  修复API问题                                    │
└─────────────────────────────────────────────────────────────┘
```

### 与成员B（PC端）联调

```
Day 7 - PC端联调
┌─────────────────────────────────────────────────────────────┐
│  成员C                        成员B                │
│  ┌─────────────┐              ┌─────────────┐        │
│  │ 后端API     │  ←←←←←←←←→│  PC端调用   │        │
│  │ 完善CORS   │              │  真实API   │        │
│  └─────────────┘              └─────────────┘        │
│         ↑                                            │
│         │ 修复跨域问题                              │
│  测试数据流（PC保存→移动端查看）                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 📝 Conventional Commits 提交示例

```bash
# 新功能
git commit -m "feat(auth): 实现用户登录API"
git commit -m "feat(hotels): 实现酒店列表API"
git commit -m "feat(hotels): 实现附近酒店搜索API"
git commit -m "feat(calendar): 实现日历组件框架"

# 创新功能
git commit -m "feat(location): 实现LBS附近酒店API"
git commit -m "feat(calendar): 实现已售罄日期标记"
git commit -m "feat(calendar): 实现价格日历视图"

# Bug修复
git commit -m "fix: 修复CORS跨域问题"
git commit -m "fix: 修复距离计算错误"

# 代码重构
git commit -m "refactor: 优化API错误处理"

# 测试
git commit -m "test: 完成移动端联调测试"
git commit -m "test: 完成PC端联调测试"

# 文档
git commit -m "docs: 编写API接口文档"
git commit -m "docs: 编写项目README"
```

---

## 🎯 每日检查清单

### Day 1 检查清单
- [ ] Express项目能正常启动
- [ ] 数据模型设计完成
- [ ] JSON数据文件创建
- [ ] 开发环境配置完成
- [ ] Git分支已创建并推送

### Day 2 检查清单
- [ ] 登录API可用（Postman测试）
- [ ] 注册API可用
- [ ] Token生成正确
- [ ] 身份验证中间件可用

### Day 3-4 检查清单
- [ ] 酒店列表API可用
- [ ] 酒店详情API可用
- [ ] 酒店新增API可用
- [ ] 酒店编辑API可用
- [ ] 酒店删除API可用
- [ ] 审核API可用
- [ ] 上架下架API可用
- [ ] CORS配置正确

### Day 5 检查清单（日历组件）
- [ ] 日历组件UI完成
- [ ] 日期选择功能正常
- [ ] 入住/离店联动正确
- [ ] 间夜计算正确
- [ ] 组件放到shared目录

### Day 6-7 检查清单（联调）
- [ ] 与成员A联调通过
- [ ] 与成员B联调通过
- [ ] 跨域问题解决
- [ ] 数据同步正常

### Day 8-9 检查清单（创新功能）
- [ ] 附近酒店API可用
- [ ] 距离计算正确
- [ ] 日历组件已售罄标记完成
- [ ] 价格日历视图完成
- [ ] H5/小程序适配完成

### Day 10-11 检查清单（优化）
- [ ] 错误处理统一
- [ ] 参数验证完善
- [ ] API响应格式统一
- [ ] 小程序域名配置完成

### Day 12-14 检查清单（测试+文档）
- [ ] 端到端测试通过
- [ ] 数据流测试通过
- [ ] 所有Bug修复
- [ ] README编写完成
- [ ] API文档完善
- [ ] 演示数据准备完成

---

## 📊 成员C工作量评估

| 功能模块 | 工作量 | 占比 |
|---------|--------|------|
| 环境搭建 + 数据设计 | 1天 | 7.1% |
| 认证API | 1天 | 7.1% |
| 酒店CRUD API | 2天 | 14.3% |
| 酒店管理API + CORS | 1天 | 7.1% |
| 自定义日历组件（创新） | 2天 | 14.3% |
| 前后端联调 | 2天 | 14.3% |
| LBS定位API（创新） | 1天 | 7.1% |
| API优化 + 小程序适配 | 2天 | 14.3% |
| 测试 + Bug修复 | 1天 | 7.1% |
| README + 文档 | 0.5天 | 3.6% |
| 集成验收 | 0.5天 | 3.6% |
| **总计** | **14天** | **100%** |

---

## 🔗 与其他成员的协作节点

| 时间 | 与成员A（移动端） | 与成员B（PC端） |
|-----|-------------------|----------------|
| Day 1 | - | - |
| Day 2 | - | - |
| Day 4 | 确认API接口规范<br>输出API文档 | 确认API接口规范<br>输出API文档 |
| Day 6 | **联调移动端**<br>解决API问题 | - |
| Day 7 | - | **联调PC端**<br>测试数据流 |
| Day 9 | 协助日历组件集成 | - |
| Day 11 | 测试LBS定位API | - |
| Day 14 | 集成测试验收 | 集成测试验收 |

---

## 📁 共享模块结构

```
shared/
├── components/             # 共享组件
│   └── Calendar/         # 自定义日历组件
│       ├── index.tsx
│       ├── index.scss
│       └── types.ts
├── types/               # 共享类型定义
│   ├── hotel.ts        # 酒店类型
│   ├── user.ts         # 用户类型
│   ├── api.ts          # API类型
│   └── index.ts        # 导出
├── constants/           # 共享常量
│   ├── config.ts       # 配置常量
│   └── status.ts      # 状态常量
└── utils/              # 共享工具函数
    ├── date.ts         # 日期处理
    ├── format.ts       # 格式化
    └── validation.ts   # 验证
```

---

## 📖 README 文档大纲

```markdown
# 易宿酒店预订平台

## 项目简介
基于 Taro + React + Express 的酒店预订平台...

## 技术栈
- 移动端：Taro + React + TypeScript + NutUI
- PC端：React + Vite + Ant Design + TypeScript
- 后端：Express.js + TypeScript + JSON存储

## 功能特性
- 酒店搜索、列表、详情
- 商户后台管理
- 审核发布流程
- 自定义日历组件
- 虚拟滚动列表
- LBS定位搜索

## 项目结构
\`\`\`
Trip/
├── mini-app/   # 移动端
├── admin/      # PC管理端
├── server/     # 后端API
└── shared/     # 共享代码
\`\`\`

## 快速开始

### 安装依赖
\`\`\`bash
npm install
\`\`\`

### 启动后端
\`\`\`bash
cd server
npm run dev
\`\`\`

### 启动移动端
\`\`\`bash
cd mini-app
npm run dev:h5
\`\`\`

### 启动PC端
\`\`\`bash
cd admin
npm run dev
\`\`\`

## API文档
详见 [docs/api.md](./docs/api.md)

## 开发指南
...

## 团队成员
- 成员A：移动端
- 成员B：PC端
- 成员C：后端+联调

## 许可证
MIT
```

---

## 🎓 学习资源

### Express 官方文档
- 官网：https://expressjs.com/
- 中文教程：https://www.runoob.com/expressjs

### JWT认证
- jsonwebtoken：https://github.com/auth0/node-jsonwebtoken

### CORS配置
- Express CORS：https://github.com/expressjs/cors

---

> 文档版本：v1.0
> 最后更新：2025-02-13
> 状态：待确认后开始执行
